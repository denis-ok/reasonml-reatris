let dropDelay = 25;
let moveDelay = 70;
let rotateDelay = 90;
let countDelay = 1000;

module Grid = {
  let hiddenRowsCount = 3;

  let width = 10;

  let heightVisible = 20;

  let height = heightVisible + hiddenRowsCount;
};
